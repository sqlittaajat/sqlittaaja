[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "sqlittaaja"
readme = "README.md"
requires-python = ">=3.8"
dynamic = ["version"]

[tool.setuptools]

[tool.setuptools_scm]
